spring:
  application:
    name: demo-app
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always
server:
  port: 18080
app:
  version: latest
